<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">
    <img src="https://svgsilh.com/svg/306277.svg" width="30" height="30" class="d-inline-block align-top" alt="" loading="lazy">
    Veterinária Integrativa
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
            <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
      <li class="nav-item active">
        <a class="nav-link" href="/pacientes">Pacientes <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/form">Cadastro de Pacientes</a>
      </li>
    </ul>
  </div>
</nav>
<div class="container">
    <br>
    <h1>Pacientes</h1>
    <br>
</div>
{{#each cachorros}}
<div class="container">
    <div class="row row-cols-1 row-cols-md-2">
        <div class="col mb-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{dataValues.nome}}</h5>
                    <small>{{dataValues.createdAt}}</small>
                    <p class="card-text">{{dataValues.idade}} Anos</p>
                    <p class="card-text">{{dataValues.raca}}</p>
                    <p class="card-text">{{dataValues.peso}} kg</p>
                    <p class="card-text">{{dataValues.tutor}}</p>
                    <p class="card-text">{{dataValues.motivo}}</p>
                    <a href="/delet/{{dataValues.id}}"><button type="button" class="btn btn-danger">Excluir</button></a>
                    <a href="/edit/{{dataValues.id}}"><button type="button" class="btn btn-primary">Editar</button></a>
                </div>
            </div>
        </div>
    </div>
</div>   
{{/each}}